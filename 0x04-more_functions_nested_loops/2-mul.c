#include "main.h"
/**
 * mul - muliplies 2 digits
 * @a: the character to multiply
 * @b: the character to multiply
 *
 * Return: product of the characters
 */
int mul(int a, int b)
{
return (a * b);
}
