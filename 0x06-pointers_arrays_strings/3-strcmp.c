#include<string.h>
#include "main.h"
/**
 * _strcmp - compares strings
 * @s1: string to be compared
 * @s2: string to be compared
 * Return: returninteger comparison
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
